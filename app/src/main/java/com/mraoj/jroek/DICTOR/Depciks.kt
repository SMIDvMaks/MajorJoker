package com.mraoj.jroek.DICTOR

import android.util.Base64
var Dep = Depciks()

class Depciks {
    fun Muoq(kiae:String): String {
        val jok = String(Base64.decode(kiae,Base64.DEFAULT))
        return jok
    }
}